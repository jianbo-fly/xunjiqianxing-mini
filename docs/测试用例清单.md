# 测试用例清单

> 整理时间：2026-04-06（补充：2026-04-12）
> 覆盖范围：小程序端 / App 后端 / Admin 后端 / 微服务
> 总计：**396 个测试用例**

---

## 汇总

| 模块 | 方法数 | 测试用例数 |
|------|--------|-----------|
| 后端 Controller（xj-app / xj-admin） | 32 | 102 |
| 后端 Service（order / admin） | 26 | 56 |
| 小程序页面逻辑 | 63 | 97 |
| ~~原合计~~ | ~~121~~ | ~~255~~ |
| **补充：后端 Controller** | — | **27** |
| **补充：后端 Admin** | — | **15** |
| **补充：小程序页面逻辑** | — | **99** |
| **合计** | **121+** | **396** |

---

## 优先级说明

| 优先级 | 说明 |
|--------|------|
| 🔴 P0 | 核心业务，必须覆盖 |
| 🟠 P1 | 重要接口，尽快补充 |
| 🟡 P2 | 辅助功能，按需补充 |

---

## 一、后端 Controller 层（102 个）

### 1.1 OrderController 🔴（50 个）

#### createOrder — 创建订单
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 1 | 正常创建订单（全部字段合法） | 正常 | 返回订单号，状态为待支付 |
| 2 | 创建含成人+儿童的订单 | 正常 | 金额 = 成人价×数量 + 儿童价×数量 |
| 3 | 创建含优惠券的订单 | 正常 | discountAmount 正确扣减（TODO 实现后变绿） |
| 4 | SKU 不存在 | 异常 | 抛出 BizException "商品不存在" |
| 5 | 商品已下架 | 异常 | 抛出 BizException "商品已下架" |
| 6 | 出行日期不可预订 | 异常 | 抛出 BizException "该日期不可预订" |
| 7 | 库存不足 | 异常 | 抛出 BizException "库存不足" |
| 8 | 出行人列表为空 | 异常 | 抛出 BizException "出行人不能为空" |

#### list — 订单列表
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 9 | 查询所有订单（不传 status） | 正常 | 返回当前用户全部订单 |
| 10 | 按 status=0 筛选待支付 | 正常 | 只返回待支付订单 |
| 11 | 分页查询（page=2, pageSize=5） | 正常 | 返回第二页数据 |
| 12 | 用户无订单时 | 异常 | 返回空列表，total=0 |
| 13 | 分页越界（page 超出总页数） | 异常 | 返回空列表，不报错 |

#### detail — 订单详情
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 14 | 查询自己的订单 | 正常 | 返回完整订单信息 |
| 15 | 出行人身份证、手机号已脱敏 | 正常 | idCard 中间位为 * |
| 16 | 订单不存在 | 异常 | 返回 404 / BizException |
| 17 | 查询他人订单 | 异常 | 抛出 BizException "无权操作" |
| 18 | 订单 ID 为负数 | 异常 | 参数校验失败 |

#### cancel — 取消订单
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 19 | 成功取消待支付订单 | 正常 | 订单状态变为已取消 |
| 20 | 取消后库存释放 | 正常 | 路线对应日期库存 +1 |
| 21 | 订单不存在 | 异常 | BizException |
| 22 | 已支付订单不允许取消 | 异常 | BizException "当前状态不允许取消" |
| 23 | 取消他人订单 | 异常 | BizException "无权操作" |

#### refund — 申请退款
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 24 | 已预订订单成功申请退款 | 正常 | 订单状态变为退款申请中(5) |
| 25 | 出行中订单不允许退款 | 异常 | BizException "当前状态不允许退款" |
| 26 | 待支付订单不允许退款 | 异常 | BizException |
| 27 | 订单不存在 | 异常 | BizException |
| 28 | 申请他人订单退款 | 异常 | BizException "无权操作" |

#### 辅助方法
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 29 | maskIdCard：正常 18 位身份证 | 正常 | 前 4 后 4 可见，中间为 * |
| 30 | maskIdCard：输入为 null | 异常 | 返回空字符串 |
| 31 | maskPhone：正常 11 位手机号 | 正常 | 前 3 后 4 可见，中间为 * |
| 32 | maskPhone：输入为 null | 异常 | 返回空字符串 |

---

### 1.2 UserController 🟠（19 个）

#### loginByCode — 微信登录
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 33 | 新用户首次登录 | 正常 | 创建用户，needsProfile=true |
| 34 | 老用户重复登录 | 正常 | 不创建新用户，返回 token |
| 35 | 微信 API 调用失败 | 异常 | 返回登录失败错误 |

#### getUserInfo — 获取用户信息
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 36 | 返回完整用户信息 | 正常 | 包含会员、领队、推广员标志 |
| 37 | 用户不存在 | 异常 | BizException |

#### updateUserInfo — 更新用户信息
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 38 | 更新昵称 | 正常 | 昵称变更成功 |
| 39 | 更新头像 | 正常 | 头像 URL 更新成功 |
| 40 | 用户不存在 | 异常 | BizException |

#### bindPhoneByWx — 微信一键绑定手机
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 41 | 成功绑定手机号 | 正常 | 用户 phone 字段更新 |
| 42 | 手机号已被其他账号绑定 | 异常 | BizException "手机号已被使用" |
| 43 | 微信返回空手机号 | 异常 | BizException |

#### bindPhone — 验证码绑定手机
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 44 | 成功绑定手机号 | 正常 | 用户 phone 字段更新 |
| 45 | 手机号已被绑定 | 异常 | BizException |
| 46 | 验证码校验失败 | 异常 | BizException（TODO 实现后生效） |

#### sendCode — 发送验证码
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 47 | 调用发送接口 | 正常 | 返回成功（TODO 实现后验证真实发送） |
| 48 | 手机号为空 | 异常 | 参数校验失败 |

#### logout — 退出登录
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 49 | 正常退出 | 正常 | token 失效 |

---

### 1.3 PromoterController 🟠（26 个）

#### info — 获取推广员信息
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 50 | 已通过审核的推广员查询信息 | 正常 | 返回完整推广员信息 |
| 51 | 用户未申请推广员 | 异常 | 返回 null 或空 |

#### apply — 申请推广员
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 52 | 首次申请 | 正常 | 创建推广员记录，status=待审核 |
| 53 | 重复申请（已有记录）更新信息 | 正常 | 更新已有记录 |
| 54 | 手机号未绑定 | 异常 | BizException |

#### qrcode — 获取推广码
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 55 | 已审核通过的推广员获取二维码 | 正常 | 返回图片字节流 |
| 56 | 推广员未通过审核 | 异常 | BizException |
| 57 | 微信 API 生成二维码失败 | 异常 | 返回错误信息 |

#### scan — 扫码上报
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 58 | 有效推广码上报 | 正常 | 记录扫码事件 |
| 59 | 空推广码上报 | 异常 | 忽略或返回失败 |

#### bind — 绑定推广员
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 60 | 成功绑定有效推广码 | 正常 | 用户绑定推广员关系建立 |
| 61 | 推广码无效 | 异常 | BizException |
| 62 | 已绑定过推广员 | 异常 | BizException |

#### statistics — 推广统计
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 63 | 返回完整统计数据 | 正常 | scanCount/orderCount/availableCommission 正确 |
| 64 | 无推广数据时 | 正常 | 各项计数为 0 |

#### withdraw — 申请提现
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 65 | 成功申请提现 | 正常 | 创建提现记录，佣金冻结 |
| 66 | 提现金额超过可用佣金 | 异常 | BizException |
| 67 | 提现金额为 0 | 异常 | 参数校验失败 |

#### commissions — 佣金记录
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 68 | 分页获取佣金记录 | 正常 | 返回当前推广员佣金列表 |
| 69 | 无佣金记录 | 正常 | 返回空列表 |

#### scanList / orderList
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 70 | 分页获取扫码记录 | 正常 | 返回扫码列表 |
| 71 | 分页获取推广订单记录 | 正常 | 返回推广订单列表 |
| 72 | 无记录时返回空列表 | 正常 | total=0 |

#### withdraws — 提现记录
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 73 | 分页获取提现记录 | 正常 | 返回提现列表 |
| 74 | 无提现记录 | 正常 | 返回空列表 |
| 75 | 记录按时间倒序排列 | 正常 | 最新记录在前 |

---

### 1.4 MessageController 🟡（7 个）

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 76 | 获取全部消息列表（分页） | 正常 | 返回消息列表 |
| 77 | 按 category 筛选消息 | 正常 | 只返回指定分类 |
| 78 | 无消息时返回空列表 | 正常 | total=0 |
| 79 | 标记单条消息已读 | 正常 | 该消息 read=true |
| 80 | 标记全部消息已读（id=all） | 正常 | 全部消息 read=true |
| 81 | 消息 ID 非法（非数字且非 "all"） | 异常 | 返回参数错误 |
| 82 | 获取未读消息数 | 正常 | 返回正确未读数量 |

---

## 二、后端 Service 层（56 个）

### 2.1 OrderServiceImpl 🔴（34 个）

#### createOrder
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 83 | 正常创建，生成唯一订单号 | 正常 | orderNo 不为空且唯一 |
| 84 | 并发创建，库存不足时回滚 | 异常 | 抛出异常，库存不变 |
| 85 | 创建后自动写入订单日志 | 正常 | OrderLog 记录 fromStatus=null, toStatus=0 |

#### cancelOrder
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 86 | 成功取消待支付订单 | 正常 | 状态变为 CANCELLED(4) |
| 87 | 释放库存 | 正常 | 库存 +1 |
| 88 | 订单不存在 | 异常 | BizException |
| 89 | 非订单创建者取消 | 异常 | BizException "无权操作" |
| 90 | 已支付订单不允许取消 | 异常 | BizException "当前状态不允许" |
| 91 | 并发取消（乐观锁失败） | 异常 | BizException 或重试 |

#### paySuccess
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 92 | 支付成功更新状态为已预订 | 正常 | 状态变为 BOOKED(1) |
| 93 | 写入支付流水号 | 正常 | payTradeNo 字段更新 |
| 94 | 重复回调（订单已支付） | 异常 | 幂等处理，不报错 |

#### applyRefund
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 95 | 已预订状态申请退款 | 正常 | 状态变为 REFUND_APPLY(5)，生成退款单 |
| 96 | 出行中状态不允许退款 | 异常 | BizException "当前状态不允许退款" |
| 97 | 非订单创建者申请 | 异常 | BizException "无权操作" |
| 98 | 订单不存在 | 异常 | BizException |

#### updateStatus
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 99 | 正常更新状态 | 正常 | 状态更新成功，返回 true |
| 100 | fromStatus 不匹配（并发冲突） | 异常 | 返回 false |

#### pageUserOrders
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 101 | 查全部订单 | 正常 | 返回用户所有订单 |
| 102 | 按状态筛选 | 正常 | 只返回指定状态订单 |
| 103 | 用户无订单 | 正常 | 返回空列表 |

#### 定时任务相关
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 104 | getExpiredPendingOrders：返回超 30 分钟未支付订单 | 正常 | 只返回超时订单 |
| 105 | closeExpiredOrder：关闭过期订单并记录日志 | 正常 | 状态变为 CLOSED(7) |
| 106 | closeExpiredOrder：订单已被支付则不关闭 | 异常 | 幂等，不影响已支付 |
| 107 | batchMarkTraveling：批量更新为出行中 | 正常 | 状态变为 TRAVELING(2) |
| 108 | batchMarkTraveling：传入空列表 | 异常 | 不执行，不报错 |
| 109 | batchMarkCompleted：批量更新为已完成 | 正常 | 状态变为 COMPLETED(3) |

#### addLog
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 110 | 正常写入日志 | 正常 | OrderLog 记录 fromStatus/toStatus/operatorType |
| 111 | 日志写入失败不影响主流程 | 异常 | 主流程事务不回滚 |

#### maskIdCard / maskPhone
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 112 | maskIdCard 正常脱敏 | 正常 | 前 4 后 4 可见 |
| 113 | maskPhone 正常脱敏 | 正常 | 前 3 后 4 可见 |
| 114 | 输入为 null 时返回空字符串 | 异常 | 不抛出 NPE |
| 115 | 输入长度不足时的处理 | 异常 | 返回原字符串或固定占位 |
| 116 | getBookedOrdersToTravel：出行日期 ≤ 今天的已预订订单 | 正常 | 返回符合条件的订单 |

---

### 2.2 AdminRefundService 🔴（13 个）

#### pageList
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 117 | 按退款编号精确查询 | 正常 | 返回匹配记录 |
| 118 | 按订单编号查询 | 正常 | 返回匹配记录 |
| 119 | 按时间范围+状态组合筛选 | 正常 | 返回符合条件记录 |
| 120 | 无退款记录 | 正常 | 返回空列表 |

#### getDetail
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 121 | 返回完整退款 VO（含用户/订单信息） | 正常 | VO 字段完整 |
| 122 | 退款记录不存在 | 异常 | BizException |

#### audit
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 123 | 审核通过：订单状态变为已退款 | 正常 | status=REFUNDED(6) |
| 124 | 审核通过：调用支付服务退款 | 正常 | refundTradeNo 写入 |
| 125 | 审核通过：发送退款成功消息 | 正常 | 消息推送调用一次 |
| 126 | 审核驳回：订单状态恢复为已预订 | 正常 | status=BOOKED(1) |
| 127 | 审核驳回：未填写备注 | 异常 | BizException "驳回时必须填写备注" |
| 128 | 重复审核 | 异常 | BizException "该退款已审核" |
| 129 | 支付服务退款失败（应回滚审批）| 异常 | 主事务回滚，退款单状态不变 |

---

### 2.3 AdminOrderService 🟠（9 个）

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 130 | 管理员查询全部订单 | 正常 | 返回所有订单 |
| 131 | 供应商只能查询自己的产品订单 | 正常 | 过滤非己产品订单 |
| 132 | 按订单号精确查询 | 正常 | 返回匹配订单 |
| 133 | 按状态+日期范围组合筛选 | 正常 | 返回符合条件订单 |
| 134 | 获取订单详情（含出行人脱敏） | 正常 | 身份证、手机号已脱敏 |
| 135 | 获取订单详情（含退款信息） | 正常 | 退款字段正确填充 |
| 136 | 订单不存在时获取详情 | 异常 | BizException |
| 137 | 添加备注 | 正常 | remark 字段更新 |
| 138 | 获取订单统计（各状态数量/今日销售） | 正常 | 数据准确 |

---

## 三、小程序页面逻辑（97 个）

### 3.1 pages/order/confirm/index.js 🔴（45 个）

#### calcAmount — 金额计算
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 139 | 仅成人：总价 = 成人价 × 成人数 | 正常 | 金额正确 |
| 140 | 成人 + 儿童：分别计算后求和 | 正常 | 金额正确 |
| 141 | 使用满减券（满足门槛） | 正常 | 优惠金额 = 券面额 |
| 142 | 使用满减券（不满足门槛） | 正常 | 优惠为 0 |
| 143 | 使用折扣券 | 正常 | 优惠金额 = 原价 × (1-折扣) |
| 144 | 全部参数为 0 | 异常 | 总价为 0，不报错 |

#### handleSubmit — 提交订单
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 145 | 正常提交，创建订单并发起支付 | 正常 | 跳转支付流程 |
| 146 | 防重复提交（submitting=true） | 正常 | 第二次点击被忽略 |
| 147 | 成人出行人未填满 | 异常 | 提示"请填写全部成人出行人" |
| 148 | 儿童出行人未填满 | 异常 | 提示"请填写全部儿童出行人" |
| 149 | 联系人姓名为空 | 异常 | 提示"请填写联系人姓名" |
| 150 | 联系人手机号为空 | 异常 | 提示"请填写联系人手机号" |
| 151 | 创建订单 API 返回失败 | 异常 | 提示错误信息，submitting 重置 |

#### doPay — 发起支付
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 152 | 支付成功跳转结果页 | 正常 | navigateTo 结果页，携带 orderId |
| 153 | 用户主动取消支付（errCode=-2） | 正常 | 不提示错误，停留当前页 |
| 154 | 支付 API 返回失败 | 异常 | 提示"支付失败" |

#### handleSelectTraveler — 选择出行人
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 155 | 选择新出行人填入空位 | 正常 | travelers 对应位置赋值 |
| 156 | 选择已被占用的出行人（替换） | 正常 | 旧值替换为新值 |
| 157 | 选择已选中的出行人（重复） | 异常 | 提示"该出行人已选择" |

#### handleToggleContact — 切换联系人
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 158 | 勾选"同出行人"：自动填充第一位成人姓名和手机 | 正常 | contact 字段更新 |
| 159 | 取消勾选：清空 contact 字段 | 正常 | contact = {} |

#### handleSelectCoupon — 选择优惠券
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 160 | 首次选择优惠券 | 正常 | selectedCoupon 赋值，重新计算金额 |
| 161 | 再次点击同一优惠券（取消） | 正常 | selectedCoupon 清空 |

#### loadData — 加载页面数据
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 162 | 并行加载路线和出行人 | 正常 | 两个请求同时发出 |
| 163 | 仅有一个成人时自动填充默认出行人 | 正常 | travelers[0] 已填充 |
| 164 | 路线不存在 | 异常 | 提示错误并返回 |

#### refreshTravelerList — 刷新出行人
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 165 | 检测到新增出行人，自动填充空位 | 正常 | 新出行人填入第一个空位 |
| 166 | 无新增出行人，列表不变 | 正常 | travelers 不变 |
| 167 | API 调用失败 | 异常 | 不崩溃，仅 console.error |

#### 其他方法
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 168 | onLoad：正确解析路由参数（routeId/skuId/date） | 正常 | data 中参数赋值正确 |
| 169 | onShow：返回页面时刷新出行人列表 | 正常 | refreshFlag=true 时触发刷新 |
| 170 | handleRemoveTraveler：清空指定位置 | 正常 | travelers[index] 置为 null |
| 171 | handleTogglePriceDetail：切换价格明细展示 | 正常 | showPriceDetail 取反 |
| 172 | handleToggleInsurance：切换意外险 | 正常 | insuranceSelected 取反 |
| 173 | handleBack：返回上一页 | 正常 | navigateBack 调用 |
| 174 | handleNoCoupon：不使用优惠券 | 正常 | selectedCoupon=null，重新计算 |
| 175 | handleShowTravelerPopup：正确解析 type 和 index | 正常 | popup 显示，参数正确 |
| 176 | handleCloseTravelerPopup：隐藏弹窗 | 正常 | showTravelerPopup=false |
| 177 | handleShowCouponPopup：打开弹窗 | 正常 | showCouponPopup=true |
| 178 | handleCloseCouponPopup：关闭弹窗 | 正常 | showCouponPopup=false |
| 179 | handleShowAddTraveler：设置 refreshFlag 并跳转 | 正常 | navigateTo 出行人页面 |
| 180 | loadUsableCoupons：无可用优惠券 | 正常 | coupons=[] |
| 181 | loadUsableCoupons：API 异常 | 异常 | 不崩溃 |
| 182 | handleContactInput：实时更新联系人字段 | 正常 | contact[field] 赋值 |
| 183 | handleContactInput：输入手机号格式错误 | 异常 | 不拦截（仅提示在 submit 时）|

---

### 3.2 pages/order/list/index.js 🟠（38 个）

#### formatOrder — 格式化订单
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 184 | status=0 时 showPayBtn=true, showCancelBtn=true | 正常 | 按钮状态正确 |
| 185 | status=1 时 showRefundBtn=true | 正常 | 退款按钮显示 |
| 186 | status=2 时 showRefundBtn=false | 正常 | 出行中不显示退款按钮 |
| 187 | status=3/4/6/7 时 showBuyAgainBtn=true | 正常 | 再次购买按钮显示 |
| 188 | 根据 status 返回正确图标路径 | 正常 | statusIcon 路径正确 |
| 189 | amount 为 null 时不崩溃 | 异常 | 显示 "0.00" 或空 |

#### formatDate / formatDateTime / getPeopleText
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 190 | formatDate：正常日期返回 "MM/DD" | 正常 | 格式正确 |
| 191 | formatDate：null 返回空字符串 | 异常 | 不崩溃 |
| 192 | formatDateTime：返回 "YYYY-MM-DD HH:MM" | 正常 | 格式正确 |
| 193 | getPeopleText：仅成人 "2位成人" | 正常 | 文本正确 |
| 194 | getPeopleText：成人+儿童 "2位成人 1位儿童" | 正常 | 文本正确 |
| 195 | getPeopleText：全为 0 | 异常 | 返回空或 "0位成人" |

#### loadList — 加载列表
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 196 | 首次加载（refresh=true）清空列表 | 正常 | list 从空开始 |
| 197 | 加载更多（refresh=false）追加数据 | 正常 | list 累积增加 |
| 198 | 最后一页时 hasMore=false | 正常 | 上拉不再触发加载 |
| 199 | 无订单数据 | 正常 | list=[], showEmpty=true |
| 200 | API 失败 | 异常 | 提示错误，loading 重置 |

#### handleTabChange
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 201 | 切换到不同 tab，重置分页刷新 | 正常 | page=1，list 清空 |
| 202 | 点击当前已选 tab | 正常 | 无操作或重新加载 |

#### handleCancel
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 203 | 确认取消后 API 成功，刷新列表 | 正常 | 列表中该订单状态更新 |
| 204 | 用户点击"不取消" | 正常 | 订单不变 |
| 205 | API 失败 | 异常 | 提示错误 |

#### handlePay
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 206 | 支付成功，刷新列表 | 正常 | 列表中订单状态更新 |
| 207 | 用户取消支付（errCode=-2） | 正常 | 不提示错误 |
| 208 | 支付失败 | 异常 | 提示"支付失败" |

#### 其他
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 209 | onPullDownRefresh：触发刷新，完成后停止下拉动画 | 正常 | stopPullDownRefresh 调用 |
| 210 | onReachBottom：触发 loadMore | 正常 | page+1 请求 |
| 211 | loadMore：正在加载时不重复触发 | 正常 | 请求不重叠 |
| 212 | handleOrderTap：携带 id 跳转详情页 | 正常 | URL 含 id 参数 |
| 213 | handleRefund：跳转退款页 | 正常 | navigateTo 退款页 |
| 214 | handleBuyAgain：携带 productId 跳转路线页 | 正常 | URL 含 productId |
| 215 | handleBuyAgain：productId 为空 | 异常 | 不跳转或提示 |
| 216 | goExplore：跳转首页 | 正常 | navigateTo 首页 |
| 217 | onShow：未登录时跳转登录页 | 正常 | redirectTo 登录页 |
| 218 | refreshList：重置 page=1 并调用 loadList | 正常 | 从第一页开始加载 |
| 219 | onLoad：计算导航栏高度 | 正常 | navHeight 赋值 |
| 220 | onLoad：获取系统信息失败时使用默认值 | 异常 | 不崩溃 |
| 221 | handleContactInput — 字段实时更新 | 正常 | 对应字段更新 |

---

### 3.3 pages/promoter/center/index.js 🟡（14 个）

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 222 | onLoad：计算状态栏和导航栏高度 | 正常 | statusBarHeight / navBarHeight 赋值 |
| 223 | loadPromoterInfo：已申请推广员，加载完整数据 | 正常 | promoter 数据填充，loading=false |
| 224 | loadPromoterInfo：未申请推广员，跳转申请页 | 正常 | redirectTo 申请页 |
| 225 | loadPromoterInfo：从缓存补充昵称和头像 | 正常 | nickname/avatar 优先用缓存 |
| 226 | loadPromoterInfo：API 失败 | 异常 | 提示"加载失败"，loading=false |
| 227 | loadPromoterInfo：status=1 时自动加载二维码 | 正常 | loadQrCode 被调用 |
| 228 | loadPromoterInfo：status≠1 时不加载二维码 | 正常 | qrCodeUrl 为空 |
| 229 | loadQrCode：成功下载并转换为 base64 dataUri | 正常 | qrCodeUrl 以 data:image 开头 |
| 230 | loadQrCode：下载失败 | 异常 | 不崩溃，qrCodeUrl 保持空 |
| 231 | handleSaveImage：二维码已生成，保存成功 | 正常 | 提示"已保存到相册" |
| 232 | handleSaveImage：二维码未生成 | 异常 | 提示"推广码未生成" |
| 233 | handleSaveImage：权限被拒绝，引导去授权 | 异常 | 弹窗引导开启权限 |
| 234 | onShareAppMessage：返回含推广码的分享路径 | 正常 | path 含 promoterCode 参数 |
| 235 | onShareAppMessage：promoter 为 null 时不崩溃 | 异常 | 使用默认标题和路径 |

---

## 四、已修复问题的回归测试（20 个）

> 防止已修复 bug 在未来迭代中复现

| # | 用例名称 | 对应问题 |
|---|---------|---------|
| 236 | 出行中订单不显示退款按钮 | 出行中不允许退款 |
| 237 | 出行中订单调用退款接口返回失败 | 出行中不允许退款 |
| 238 | xj-common 包中不存在 OrderStatus 枚举 | 废弃枚举已删除 |
| 239 | 所有 Java 文件 import OrderStatus 均来自 service-order 包 | 枚举统一 |
| 240 | refundableStatuses() 只包含 BOOKED(1) | 出行中不允许退款 |
| 241–255 | （预留给后续修复的 P0/P1 问题回归测试） | 待补充 |

---

## 编写建议

### 后端测试框架
```
JUnit 5 + Mockito + Spring Boot Test
- Controller 层：MockMvc + @WebMvcTest
- Service 层：@ExtendWith(MockitoExtension.class)
- 集成测试：@SpringBootTest + H2 内存数据库
```

### 小程序测试框架
```
Jest + miniprogram-simulate
- 业务逻辑函数（calcAmount / formatOrder 等）：纯函数单测
- Page 生命周期：miniprogram-simulate 模拟
- API 调用：jest.mock() 模拟 wx 对象
```

### 执行优先级

| 阶段 | 内容 | 用例数 |
|------|------|-------|
| 第一阶段 | OrderServiceImpl + AdminRefundService + handleSubmit + calcAmount + **登录流程** | ~80 个 |
| 第二阶段 | 所有 Controller + order/list 页面逻辑 + **路线详情** | ~140 个 |
| 第三阶段 | promoter/confirm 剩余方法 + 回归测试 + **旅行页/搜索/消息/会员** | ~176 个 |

---

## ⚠️ 发现的 BUG（需优先修复）

> 补充时间：2026-04-12

| # | 位置 | 问题 | 严重程度 |
|---|------|------|---------|
| 1 | `pages/member/index/index.js` | 调用 `orderApi.getCounts()` 但该方法**不存在** | 🔴 运行时崩溃 |
| 2 | `pages/member/index/index.js` | 调用 `customApi.getCounts()` 但该方法**不存在** | 🔴 运行时崩溃 |
| 3 | `UserController.phoneLogin()` | SMS 验证码校验为 TODO，任意 code 可登录 | 🔴 安全漏洞 |
| 4 | `UserController.bindPhone()` | 同上，验证码未实际校验 | 🔴 安全漏洞 |

---

## 五、补充：后端 Controller 层（27 个）

> 补充时间：2026-04-12

### 5.1 UserController — 新增接口 🔴（12 个）

#### phoneLogin — 手机号登录
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 256 | 手机号+验证码正常登录（新用户） | 正常 | 创建用户（openid=`phone_<phone>`），返回 token |
| 257 | 手机号已存在，直接登录（老用户） | 正常 | 不创建新用户，返回已有 token |
| 258 | 验证码错误（TODO 实现后生效） | 异常 | BizException "验证码错误" |
| 259 | 手机号格式非法 | 异常 | 参数校验失败 |

#### loginByWxAll — 微信一键登录+获取手机号
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 260 | 新用户一键登录+绑定手机号 | 正常 | 返回 token + hasPhone=true |
| 261 | 手机号已被其他 openid 绑定 | 异常 | BizException "手机号冲突" |
| 262 | 微信 phoneCode 无效 | 异常 | BizException |

#### loginByCode — 补充 hasPhone 字段
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 263 | 新用户返回 hasPhone=false | 正常 | 前端进入手机绑定流程 |
| 264 | 老用户已绑手机返回 hasPhone=true | 正常 | 前端直接登录成功 |

#### bindPhone — pendingToken 场景
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 265 | pendingToken 通过 header 传递，绑定成功 | 正常 | 用户 phone 字段更新 |
| 266 | 无 Authorization header | 异常 | 鉴权失败 |

#### needsProfile 判断
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 267 | 头像为 `http://tmp` 占位符 | 正常 | needsProfile=true |

---

### 5.2 推广员路由守卫 🔴（8 个）

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 268 | apply 页 checkExistingApplication：status=0 跳转审核页 | 正常 | redirectTo status |
| 269 | apply 页 checkExistingApplication：status=1 跳转推广中心 | 正常 | redirectTo center |
| 270 | apply 页：无申请记录，展示表单 | 正常 | 表单正常显示，预填用户信息 |
| 271 | status 页：status=1 自动跳转推广中心 | 正常 | redirectTo center |
| 272 | status 页：无申请记录，提示并返回 | 正常 | toast + navigateBack |
| 273 | center 页：status=0 跳转审核页 | 正常 | redirectTo status |
| 274 | status 页 formatDateTime：ISO 格式转换 | 正常 | "2026-04-12T10:30:00" → "2026-04-12 10:30" |
| 275 | 三页循环跳转防护（status 变更时） | 异常 | 不出现死循环 |

---

### 5.3 sendCode — 频率限制 🟠（2 个）

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 276 | 60 秒内重复发送 | 异常 | 频率限制拦截 |
| 277 | 超过 60 秒后重新发送 | 正常 | 发送成功 |

---

### 5.4 PromoterController (Admin) 🔴（5 个）

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 278 | 审核通过推广员申请 | 正常 | status=0→1，生成 promoCode |
| 279 | 审核驳回推广员申请+备注 | 正常 | status→rejected |
| 280 | 待审核数量统计 | 正常 | pendingApplyCount 正确 |
| 281 | 推广员启用/停用 | 正常 | 状态切换 |
| 282 | 提现审核 | 正常 | 金额校验+状态流转 |

---

## 六、补充：后端 Admin 层（15 个）

> 补充时间：2026-04-12

### 6.1 Admin 通用 🟠

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 283 | Admin 登录鉴权（Sa-Token） | 正常 | token 校验通过 |
| 284 | Admin 未登录访问接口 | 异常 | 401 拦截 |
| 285 | 供应商权限隔离：只能查看自己产品的订单 | 正常 | 过滤非己产品 |

### 6.2 Admin 定制需求管理 🟠

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 286 | 定制需求列表（按状态筛选） | 正常 | 返回对应状态数据 |
| 287 | 定制需求跟进（状态变更+备注） | 正常 | 状态更新 |
| 288 | 定制需求详情 | 正常 | 完整信息 |

### 6.3 Admin 路线管理 🟠

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 289 | 路线列表（分页+筛选） | 正常 | 返回路线数据 |
| 290 | 路线上架/下架 | 正常 | 状态切换 |
| 291 | 路线库存管理 | 正常 | 日期维度库存更新 |

### 6.4 Admin 其他模块 🟡

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 292 | 优惠券发放 | 正常 | 创建券记录 |
| 293 | 优惠券核销 | 正常 | 状态变更 |
| 294 | 用户列表/详情 | 正常 | 返回用户数据 |
| 295 | 数据统计面板（各项指标） | 正常 | 数据准确 |
| 296 | 推广员绑定日志查询 | 正常 | 返回绑定记录 |
| 297 | 推广员佣金记录查询 | 正常 | 返回佣金列表 |

---

## 七、补充：小程序页面逻辑（99 个）

> 补充时间：2026-04-12

### 7.1 pages/login/index.js 🔴（20 个）

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 298 | handleWxLogin：新用户（hasPhone=false）→ 进入手机绑定 | 正常 | step=2, pendingToken 存于 data（不持久化） |
| 299 | handleWxLogin：老用户（hasPhone=true）→ 直接登录 | 正常 | token 持久化，跳转首页 |
| 300 | handleWxLogin：微信 API 失败 | 异常 | toast 提示，不持久化 token |
| 301 | handlePhoneLogin：切换到手机登录 | 正常 | step=2, fromPhoneEntry=true |
| 302 | handleBindPhone（微信路径）：调用 bindPhone + pendingToken | 正常 | 绑定成功后持久化 token |
| 303 | handleBindPhone（手机路径）：调用 phoneLogin API | 正常 | 直接创建用户+登录 |
| 304 | **安全：绑定手机时按返回键** | 安全 | **不应登录成功，pendingToken 未持久化** |
| 305 | handleSendCode：手机号格式校验 `/^1\d{10}$/` | 异常 | 不合法则 toast 提示 |
| 306 | handleSendCode：60 秒倒计时 | 正常 | 按钮禁用+倒计时显示 |
| 307 | handleSendCode：倒计时期间重复点击 | 异常 | 被忽略 |
| 308 | 验证码为空时提交 | 异常 | toast "请输入验证码" |
| 309 | 手机号为空时提交 | 异常 | toast "请输入手机号" |
| 310 | onUnload：清理倒计时 timer | 正常 | 无内存泄漏 |
| 311 | onUnload：清理 pendingToken | 正常 | 不残留未持久化 token |
| 312 | 协议勾选状态切换 | 正常 | agreed 取反 |
| 313 | 未勾选协议时点击登录 | 异常 | toast 提示 |
| 314 | 微信登录 → 绑定失败 → 重试 | 异常 | pendingToken 仍可用 |
| 315 | loginSuccess：有 redirectUrl 则回源页 | 正常 | 跳转来源页 |
| 316 | loginSuccess：无 redirectUrl 则回首页 | 正常 | switchTab 首页 |
| 317 | 并发点击登录按钮 | 异常 | 防重复提交 |

---

### 7.2 pages/index/index.js 🟠（15 个）

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 318 | loadHomeData：瀑布流左右分栏 | 正常 | routes 交替分配到 leftRoutes/rightRoutes |
| 319 | tryBindPromoter：有 pendingPromoCode 时静默绑定 | 正常 | 绑定成功后清除 storage |
| 320 | tryBindPromoter：已绑定推广员 | 正常 | 静默忽略，不报错 |
| 321 | tryBindPromoter：未登录时不绑定 | 正常 | 跳过绑定逻辑 |
| 322 | handleCitySelect：城市历史去重（最多 5 条） | 正常 | 不重复，超出淘汰最旧 |
| 323 | handleCitySelect：切换城市后重置路线列表 | 正常 | 列表清空并重新加载 |
| 324 | handleBannerTap：linkType=1 跳路线详情 | 正常 | navigateTo routeDetail |
| 325 | handleBannerTap：linkType=2 跳 WebView | 正常 | navigateTo webview |
| 326 | handleBannerTap：linkType=3 自定义导航 | 正常 | 对应跳转 |
| 327 | onPageScroll：banner 滚动渐隐效果 | 正常 | scrollTop>215 时隐藏 |
| 328 | 未读消息数加载（onShow） | 正常 | 红点更新 |
| 329 | 价格展示：有原价时显示划线价 | 正常 | hasOriginal=true |
| 330 | loadHomeData：API 失败 | 异常 | toast 提示，不崩溃 |
| 331 | banner 数据为空 | 异常 | 不显示轮播，不报错 |
| 332 | 推荐路线为空 | 异常 | 显示空态 |

---

### 7.3 pages/route/detail/index.js 🔴（18 个）

#### 日历系统
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 333 | 日历加载：当月剩余日期展示 | 正常 | 正确展示可预订日期+价格 |
| 334 | 日历弹窗：懒加载（初始 2 月，最多 12 月） | 正常 | 滚动时加载更多 |
| 335 | 价格分级：低/中/高（±15% 基准） | 正常 | 颜色标识正确 |
| 336 | 过去日期库存显示为 0 | 正常 | 不可选 |
| 337 | 日历周起始：周一开始 (weekday+6)%7 | 正常 | 格子对齐正确 |
| 338 | 多种日期格式兼容（string/array/object） | 正常 | 均正确解析 |

#### 人数选择与价格
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 339 | handleAdultMinus：减到 1 时不可再减 | 正常 | 最少 1 成人 |
| 340 | handleChildPlus：儿童不能超过成人数 | 正常 | 受限提示 |
| 341 | handleAdultMinus：成人减少时自动减儿童 | 正常 | 儿童 ≤ 成人 |
| 342 | 总库存不足时加人数失败 | 异常 | toast 提示库存不足 |
| 343 | calcTotalAmount：成人价×数量+儿童价×数量 | 正常 | 金额正确 |
| 344 | 儿童价未设置时默认 70% 成人价 | 正常 | 自动计算 |

#### 套餐与交互
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 345 | handlePackageSelect：切换套餐重置日期和价格 | 正常 | 清空已选日期 |
| 346 | 滚动联动：内容区滚动 ↔ 天数导航高亮 | 正常 | 对应 day card 高亮 |
| 347 | 固定导航栏：滚动 >200px 后显示 | 正常 | 导航栏不透明 |
| 348 | 收藏切换（toggle） | 正常 | API 调用正确 |
| 349 | 分享卡片含路线信息 | 正常 | title + imageUrl 正确 |
| 350 | 路线不存在 | 异常 | 提示并返回 |

---

### 7.4 pages/search/index.js 🟠（10 个）

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 351 | 搜索跳转：redirectTo 到路线列表（keyword+city） | 正常 | 正确传参 |
| 352 | 空关键词搜索 | 异常 | 拦截或提示 |
| 353 | 搜索历史保存 | 正常 | 关键词写入 storage |
| 354 | 搜索历史去重 | 正常 | 相同关键词不重复 |
| 355 | 删除单条历史 | 正常 | 列表更新 |
| 356 | 清空全部历史（需确认弹窗） | 正常 | 确认后清空 |
| 357 | 历史 JSON 解析失败 | 异常 | fallback 空数组，不崩溃 |
| 358 | 排序切换重置分页 | 正常 | page=1 |
| 359 | 热门搜索词点击 | 正常 | 自动搜索 |
| 360 | 输入特殊字符 | 异常 | 不崩溃 |

---

### 7.5 pages/travel/index/index.js 🟠（18 个）

#### 跟团游（Tab 0）
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 361 | 分类筛选（国内/出境/周边） | 正常 | 正确过滤 |
| 362 | 天数筛选（minDays/maxDays） | 正常 | 范围正确 |
| 363 | 价格筛选（filterMinPrice/filterMaxPrice） | 正常 | 范围正确 |
| 364 | 出发城市筛选（优先筛选面板） | 正常 | 面板选择 > 搜索栏 fallback |
| 365 | 瀑布流分页加载 | 正常 | 左右交替追加 |
| 366 | 筛选面板确认/重置 | 正常 | pending 值生效/清除 |

#### 定制游（Tab 1）
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 367 | 表单必填校验顺序：目的地→时间→天数→预算→手机 | 正常 | 按序校验 |
| 368 | 目的地选"其他"需手动输入 | 正常 | 自定义目的地字段出现 |
| 369 | 选"选择日期"触发日期选择器 | 正常 | picker 弹出 |
| 370 | 成人数最少 1，儿童 ≤ 成人 | 正常 | 计数器约束 |
| 371 | 需求标签多选 | 正常 | 选中项正确传参 |
| 372 | 提交成功后重置表单 | 正常 | 全部字段清空 |
| 373 | 提交失败 | 异常 | toast 提示 |
| 374 | 未登录时提交 | 异常 | 跳转登录页 |

#### 通用
| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 375 | Tab 切换（跟团游 ↔ 定制游） | 正常 | 内容切换 |
| 376 | 定制需求列表：按状态筛选 | 正常 | tab 对应状态过滤 |
| 377 | 定制需求列表：状态文本映射（0→待处理, 1→跟进中, 2→已完成） | 正常 | 文本正确 |
| 378 | 定制需求：创建新需求跳转（tab=1） | 正常 | 参数正确 |

---

### 7.6 pages/member/index/index.js 🟠（10 个）

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 379 | 登录状态检查（onLoad + onShow） | 正常 | 未登录显示登录引导 |
| 380 | 用户信息展示（头像/昵称） | 正常 | 正确显示 |
| 381 | handlePromoterTap：已是推广员→推广中心 | 正常 | go.promoterCenter |
| 382 | handlePromoterTap：非推广员→申请页 | 正常 | go.promoterApply |
| 383 | handleLogout：确认弹窗 | 正常 | 二次确认 |
| 384 | handleLogout：清除所有 auth 数据 | 正常 | token/userInfo/globalData 全清 |
| 385 | ~~orderApi.getCounts() 调用~~ | **BUG** | **方法不存在，运行时报错** |
| 386 | ~~customApi.getCounts() 调用~~ | **BUG** | **方法不存在，运行时报错** |
| 387 | 待上线功能点击（会员/领队/优惠券） | 正常 | toast "待上线" |
| 388 | 订单入口按状态跳转 | 正常 | 携带 status 参数 |

---

### 7.7 pages/message/list/index.js 🟡（8 个）

| # | 用例名称 | 类型 | 预期结果 |
|---|---------|------|---------|
| 389 | 按分类筛选（全部/订单/系统） | 正常 | category 正确传参 |
| 390 | 点击消息：乐观标记已读 | 正常 | UI 立即更新，后台 API 静默调用 |
| 391 | 点击消息：跳转 link | 正常 | 正确导航 |
| 392 | 全部标记已读 | 正常 | 所有 item.read=true |
| 393 | 分页加载 | 正常 | 上拉追加 |
| 394 | 无消息空态 | 正常 | 显示空态组件 |
| 395 | 消息图标分类映射 | 正常 | category→icon 正确 |
| 396 | API 失败时不崩溃 | 异常 | 合理降级 |
