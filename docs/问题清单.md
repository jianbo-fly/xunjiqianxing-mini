# 系统问题清单

> 分析时间：2026-04-06
> 覆盖范围：小程序端 / App 后端 / Admin 后端 / Admin Web 前端 / 微服务

---

## P0 — 上线前必须修复

### 1. 支付系统为 Mock 假数据
- **文件**：`xj-service-payment/PaymentServiceImpl.java:76`
- **问题**：所有支付直接标记成功，未接入真实微信支付；退款通知处理完全未实现；支付记录使用虚假流水号
- **影响**：核心交易功能不可用

### 2. 短信验证码未实现
- **文件**：`xj-app/UserController.java:207,230`
- **问题**：`// TODO: 验证短信验证码` 已注释；发送短信仅打 log，不实际发送
- **影响**：任何人可绑定任意手机号，存在安全漏洞

### 3. 优惠券折扣未计算
- **文件**：`xj-app/OrderController.java:95`
- **问题**：`discountAmount` 硬编码为 `BigDecimal.ZERO`，`// TODO: 优惠券计算`
- **影响**：用户选择优惠券后实际不生效，仍按原价收费

### 4. 推广码为 trial 版本
- **文件**：`xj-app/PromoterController.java:105`
- **问题**：生成小程序码时 `env` 硬编码为 `"trial"`，`// todo：修改为release`
- **影响**：生产环境推广码无法被普通用户扫描

---

## P1 — 尽快修复

### 5. 退款调用失败被静默忽略
- **文件**：`xj-admin/AdminRefundService.java:148-163`
- **问题**：实际退款接口失败后仍继续将退款单标记为"已通过"
- **影响**：用户未收到退款，系统显示退款成功，产生资金纠纷

### 6. 出行中订单允许申请退款（已修复）
- **文件**：`xj-service-order/OrderStatus.java`、`pages/order/list/index.js`、`pages/order/detail/index.js`
- **问题**：`refundableStatuses()` 包含 `TRAVELING(2)`，前端按钮也对 status=2 显示
- **状态**：✅ 已修复，出行中订单不再允许退款

### 7. 收藏 / 搭子 / 消息模块无后端支持
- **文件**：`xj-miniprogram/config/api.js:123-144`
- **问题**：收藏、搭子全部接口标注"待后端实现"；消息模块部分接口缺失
- **影响**：相关功能入口存在但无法使用

---

## P2 — 迭代中修复

### 8. 保险选项无实际效果
- **文件**：`xj-miniprogram/pages/order/confirm/index.js:527`
- **问题**：`handleToggleInsurance()` 仅切换 UI 状态，不计入订单金额
- **影响**：用户勾选保险后订单中无对应费用

### 9. 会员年费硬编码
- **文件**：`xj-service-member/MemberServiceImpl.java:33`
- **问题**：`MEMBER_YEAR_PRICE = 99`，注释 `// TODO: 从配置表读取`
- **影响**：价格调整需改代码重新部署

### 10. 消息 ID 类型不一致
- **文件**：`xj-service-message/MessageController.java:55`
- **问题**：接口用 `String` 类型接收 ID（含特殊值 `"all"`），实际数据为数字型，类型不统一

### 11. 过滤器边界值异常
- **文件**：`xj-miniprogram/pages/route/list/index.js:13-24`
- **问题**：`10天以上` 和 `10000以上` 的 `max` 设为 `0`，后端过滤逻辑可能将其误解为"无上限"或"等于0"

### 12. 手机号格式未校验
- **文件**：`xj-miniprogram/pages/order/confirm/index.js:414`
- **问题**：联系人手机号仅判断非空，未做正则格式校验
- **影响**：非法手机号可提交成功

### 13. 优惠券字段命名不统一
- **文件**：`xj-miniprogram/pages/member/coupon/index.js:50`
- **问题**：前端同时兼容 `expireDate`/`expiredAt`、`minAmount`/`minOrderAmount`，说明后端 API 返回字段曾有变更未彻底统一

---

## P3 — 优化项

### 14. 环境配置未切换生产
- **文件**：`xj-miniprogram/config/api.js:22`
- **问题**：`env: 'dev'`，baseUrl 为 ngrok 临时隧道地址，隧道过期即全面故障

### 15. Authorization 无 Bearer 前缀
- **文件**：`xj-miniprogram/services/request.js:22`
- **问题**：请求头直接发 token，未使用标准 `Bearer {token}` 格式

### 16. 库存扣减存在竞态窗口
- **文件**：`xj-app/OrderController.java:80-85`
- **问题**：`lockStock()` 与创建订单之间存在短暂间隙，高并发下可能超卖

### 17. 优惠券加载未等待
- **文件**：`xj-miniprogram/pages/order/confirm/index.js:177`
- **问题**：`loadUsableCoupons()` 未加 `await`，弱网环境下优惠券可能不显示

### 18. xj-common 遗留废弃枚举（已修复）
- **文件**：`xj-common/src/main/java/com/xunjiqianxing/common/enums/OrderStatus.java`
- **问题**：字符串类型的 OrderStatus 枚举零引用，与整数版本枚举并存造成歧义
- **状态**：✅ 已删除

---

## 已修复汇总

| # | 问题 | 修复时间 |
|---|------|---------|
| 6 | 出行中订单允许退款 | 2026-04-06 |
| 18 | xj-common 废弃 OrderStatus 枚举 | 2026-04-06 |

---

*本文件由代码分析自动生成，问题状态随修复进度同步更新*
