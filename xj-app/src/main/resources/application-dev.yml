# 开发环境配置

spring:
  # 数据源
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/xunjiqianxing?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false

  # Redis
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 10000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms

# 微信小程序配置
wechat:
  miniapp:
    appid: ${WECHAT_APPID:wx02e0737068f03dac}
    secret: ${WECHAT_SECRET:4cb241b8c72ea4d9746dd3b31f0c6cf1}

# 微信支付配置
wechat-pay:
  appid: ${WECHAT_APPID:your-appid}
  mchid: ${WECHAT_MCHID:your-mchid}
  api-v3-key: ${WECHAT_API_V3_KEY:your-api-v3-key}
  cert-path: ${WECHAT_CERT_PATH:}
  key-path: ${WECHAT_KEY_PATH:}
  notify-url: ${WECHAT_PAY_NOTIFY_URL:https://your-domain.com/api/pay/notify}

# 腾讯云COS配置


# 日志配置
logging:
  level:
    com.xunjiqianxing: debug
    org.springframework.web: info
