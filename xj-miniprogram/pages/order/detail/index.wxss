/**
 * 订单详情页样式 - 对齐 Figma 5-4
 */

.page-order-detail {
  background: #f8f6f6;
  min-height: 100vh;
}

/* 加载（高度由 inline style 动态传入） */
.loading-container {
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 主滚动（高度由 JS 计算后通过 inline style 传入） */
.main-scroll {}

/* 主内容 */
.main-content {
  padding: 32rpx;
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

/* ===== 状态横幅 ===== */
.status-banner {
  border-radius: 24rpx;
  padding: 40rpx;
  position: relative;
  overflow: hidden;
}

/* 装饰圆 */
.status-banner__deco {
  position: absolute;
  width: 192rpx;
  height: 192rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.1);
  filter: blur(24rpx);
  right: -32rpx;
  top: -32rpx;
  pointer-events: none;
}

.status-banner--orange {
  background: #ec3713;
  box-shadow: 0 20rpx 30rpx -6rpx rgba(236, 55, 19, 0.2), 0 8rpx 12rpx -8rpx rgba(236, 55, 19, 0.2);
}

.status-banner--primary {
  background: linear-gradient(to right, #ec3713, #ea580c);
  box-shadow: 0 20rpx 30rpx -6rpx rgba(236, 55, 19, 0.2), 0 8rpx 12rpx -8rpx rgba(236, 55, 19, 0.2);
}

.status-banner--green {
  background: linear-gradient(to right, #10b981, #059669);
  box-shadow: 0 20rpx 30rpx -6rpx rgba(16, 185, 129, 0.2);
}

.status-banner--gray {
  background: linear-gradient(to right, #9ca3af, #6b7280);
}

.status-banner--blue {
  background: linear-gradient(to right, #3b82f6, #2563eb);
  box-shadow: 0 20rpx 30rpx -6rpx rgba(59, 130, 246, 0.2);
}

.status-banner__row {
  display: flex;
  align-items: center;
  gap: 24rpx;
  position: relative;
}

.status-banner__icon {
  width: 56rpx;
  height: 56rpx;
  flex-shrink: 0;
}

.status-banner__content {
  flex: 1;
}

.status-banner__title {
  font-size: 40rpx;
  font-weight: 600;
  color: #ffffff;
  line-height: 56rpx;
}

.status-banner__subtitle {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.9);
  line-height: 40rpx;
  margin-top: 4rpx;
}

.status-countdown {
  margin-top: 20rpx;
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.9);
  position: relative;
}

.status-countdown__time {
  font-weight: 600;
  color: #ffffff;
}

/* ===== 通用卡片 ===== */
.card {
  background: #ffffff;
  border-radius: 24rpx;
  border: 1rpx solid #f3f4f6;
  box-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.05);
  padding: 34rpx;
}

/* ===== 区块标题（红竖条 + 文字） ===== */
.section-hd {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-bottom: 24rpx;
}

.section-hd__bar {
  width: 8rpx;
  height: 32rpx;
  border-radius: 9999rpx;
  background: #ec3713;
  flex-shrink: 0;
}

.section-hd__title {
  font-size: 32rpx;
  font-weight: 600;
  color: #111827;
}

/* ===== 路线卡片 ===== */
.route-card {
  display: flex;
  align-items: flex-start;
  gap: 32rpx;
}

.route-img {
  width: 192rpx;
  height: 192rpx;
  border-radius: 16rpx;
  flex-shrink: 0;
  background: #e5e7eb;
}

.route-info {
  flex: 1;
  min-width: 0;
  padding-top: 4rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.route-name {
  font-size: 32rpx;
  font-weight: 500;
  color: #111827;
  line-height: 40rpx;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
  overflow: hidden;
}

.route-meta {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.route-meta-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.route-meta-icon {
  width: 24rpx;
  height: 26rpx;
  flex-shrink: 0;
}

.route-meta-text {
  font-size: 24rpx;
  color: #6b7280;
}

/* ===== 出行人 ===== */
.traveler-item {
  display: flex;
  align-items: center;
  gap: 24rpx;
  padding-bottom: 26rpx;
  margin-bottom: 24rpx;
}

.traveler-item--border {
  border-bottom: 1rpx solid #f9fafb;
}

.traveler-item:last-child {
  padding-bottom: 0;
  margin-bottom: 0;
}

.traveler-avatar {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 500;
  flex-shrink: 0;
}

.traveler-avatar--adult {
  background: #fff0ed;
  color: #ec3713;
}

.traveler-avatar--child {
  background: #eff6ff;
  color: #2563eb;
}

.traveler-detail {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.traveler-name-row {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.traveler-name {
  font-size: 32rpx;
  font-weight: 500;
  color: #1f2937;
}

.type-badge {
  padding: 3rpx 12rpx;
  border-radius: 8rpx;
  font-size: 24rpx;
}

.type-badge--adult {
  background: #f3f4f6;
  color: #6b7280;
}

.type-badge--child {
  background: #eff6ff;
  color: #2563eb;
}

.traveler-id {
  font-size: 24rpx;
  color: #9ca3af;
}

/* ===== 键值对行（联系人） ===== */
.kv-rows {
  display: flex;
  flex-direction: column;
}

.kv-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 0;
}

.kv-row:first-child {
  padding-top: 0;
}

.kv-row:last-child {
  padding-bottom: 0;
}

.kv-label {
  font-size: 28rpx;
  color: #6b7280;
}

.kv-value {
  font-size: 28rpx;
  color: #111827;
}

/* ===== 费用明细 ===== */
.price-rows {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.price-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.price-label {
  font-size: 28rpx;
  color: #6b7280;
}

.price-val {
  font-size: 28rpx;
  color: #111827;
}

.price-val--discount {
  color: #ec3713;
}

.price-total-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 26rpx;
  margin-top: 10rpx;
  border-top: 1rpx solid #f3f4f6;
}

.price-total-label {
  font-size: 28rpx;
  font-weight: 500;
  color: #111827;
}

.price-total-val {
  font-size: 48rpx;
  font-weight: 700;
  color: #ec3713;
}

/* ===== 订单信息 - 复制按钮 ===== */
.order-no-row {
  display: flex;
  align-items: center;
  gap: 12rpx;
  flex: 1;
  justify-content: flex-end;
  min-width: 0;
}

.order-no-text {
  flex: 1;
  text-align: right;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.copy-btn {
  padding: 6rpx 16rpx;
  background: rgba(236, 55, 19, 0.05);
  border: 1rpx solid rgba(236, 55, 19, 0.2);
  border-radius: 8rpx;
  flex-shrink: 0;
}

.copy-btn__text {
  font-size: 24rpx;
  color: #ec3713;
}

/* ===== 商家信息 ===== */
.merchant-row {
  display: flex;
  align-items: center;
  gap: 24rpx;
}

.merchant-avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background: #f3f4f6;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.merchant-avatar__text {
  font-size: 28rpx;
  color: #6b7280;
  font-weight: 500;
}

.merchant-info {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.merchant-name {
  font-size: 30rpx;
  font-weight: 500;
  color: #111827;
}

.merchant-desc {
  font-size: 24rpx;
  color: #9ca3af;
}

.merchant-call {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: linear-gradient(to right, #ec3713, #ea580c);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.merchant-call__text {
  font-size: 22rpx;
  color: #ffffff;
  font-weight: 500;
}

/* ===== 底部占位 ===== */
.bottom-placeholder {
  height: calc(180rpx + env(safe-area-inset-bottom));
}

/* ===== 底部操作栏 ===== */
.bottom-bar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  background: #ffffff;
  border-top: 1rpx solid #f3f4f6;
  box-shadow: 0 -8rpx 12rpx -2rpx rgba(0, 0, 0, 0.05);
  padding: 26rpx 32rpx;
  padding-bottom: calc(26rpx + env(safe-area-inset-bottom));
  z-index: 100;
}

.bottom-bar__btns {
  display: flex;
  gap: 24rpx;
  align-items: center;
}

.bbar-btn {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 26rpx 0;
  border: 1rpx solid #e5e7eb;
  border-radius: 16rpx;
}

.bbar-btn__text {
  font-size: 28rpx;
  font-weight: 500;
  color: #4b5563;
}

.bbar-btn--primary {
  background: linear-gradient(to right, #ec3713, #ea580c);
  border-color: transparent;
}

.bbar-btn--primary .bbar-btn__text {
  color: #ffffff;
}
