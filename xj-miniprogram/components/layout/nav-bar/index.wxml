<view
  class="nav-bar nav-bar--{{theme}} {{fixed ? 'nav-bar--fixed' : ''}} {{border ? 'nav-bar--border' : ''}} {{customClass}}"
  style="background: {{background}};"
>
  <!-- 状态栏占位 -->
  <view style="height: {{statusBarHeight}}px;"></view>

  <!-- 导航栏内容 -->
  <view class="nav-bar__content" style="height: {{navBarHeight}}px;">
    <!-- 左侧区域 -->
    <view class="nav-bar__left">
      <view wx:if="{{showBack}}" class="nav-bar__btn" bindtap="handleBack">
        <view class="nav-bar__back-icon"></view>
      </view>
      <view wx:if="{{showHome}}" class="nav-bar__btn" bindtap="handleHome">
        <view class="nav-bar__home-icon"></view>
      </view>
      <slot name="left"></slot>
    </view>

    <!-- 标题区域 -->
    <view class="nav-bar__title">
      <text wx:if="{{title}}" style="color: #111827;">{{title}}</text>
      <slot name="title"></slot>
    </view>

    <!-- 右侧区域 -->
    <view class="nav-bar__right">
      <slot name="right"></slot>
    </view>
  </view>
</view>

<!-- 占位元素（fixed时使用） -->
<view
  wx:if="{{fixed}}"
  class="nav-bar__placeholder"
  style="height: {{statusBarHeight + navBarHeight}}px;"
></view>
